syntax = "proto3";

package consumer;
option go_package = "./consumer";



message PingRequest {
  string ping = 1;
}

message PingResponse {
  string pong = 1;
}

service Consumer {
  // Ping
  rpc Ping(PingRequest) returns(PingResponse);
}
