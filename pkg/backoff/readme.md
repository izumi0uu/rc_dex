指数退避等待

当我们与外部建立连接失败时，通常希望不立即重试（以避免向网络或服务器发送过多请求），而是采用指数退避的方式。

我们有几个参数:
1. 初始退避（在第一次失败后等待多长时间再重试）
2. 乘数（在重试失败后用于乘以退避的因子）
3. 抖动（随机化退避的程度）
4. 最大退避时间（退避上限）

指数级地延迟连接尝试的开始时间，直到达到最大延迟上限MAX_BACKOFF，并加入随机延迟。
对于有紧迫问题的实施（例如在手机上最小化唤醒次数），可能希望使用不同的算法，尤其是不同的抖动逻辑。